/*Deprecated
const mongoose = require("mongoose")
          const hack = mongoose.Schema({
            ServerId:String,
            UserId:String,
            Timeout:String,
            Muted:Number,
            Roles:Array,
            Exists:Number
        })
module.exports = mongoose.model("mute", hack)
*/